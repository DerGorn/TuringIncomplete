import React from "react";
/* import { ConnectionNode } from "./ConnectionNode";

export class On {
  id: string;
  connections: ConnectionNode;
  value: boolean;
  constructor() {
    this.value = true;
  }
  setConnections(ConnectionNode) {
    this.connections.addConnection(ConnectionNode);
  }
}

export const drawOn = (on: On, parentElement: HTMLElement) => {};
 */
